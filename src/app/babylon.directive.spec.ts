import { BabylonDirective } from './babylon.directive';

describe('BabylonDirective', () => {
  it('should create an instance', () => {
    const directive = new BabylonDirective();
    expect(directive).toBeTruthy();
  });
});
